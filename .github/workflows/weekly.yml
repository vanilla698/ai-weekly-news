name: Weekly AI Auto News Push

on:
  schedule:
    - cron: "0 0 * * 3"
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Generate news HTML
        run: python3 generate_news.py

      - name: Commit generated HTML
        run: |
          git config user.name "Mavis Bot"
          git config user.email "mavis@ai-weekly-news"
          git add ai_auto_news_preview.html
          git commit -m "📡 $(date '+%Y-%m-%d %H:%M') 自动更新情报"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send DingTalk notification
        run: python3 upload_and_push.py
        env:
          DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
          GITHUB_REPO: ${{ github.repository }}
